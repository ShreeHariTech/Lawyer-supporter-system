# ── Web Framework ──────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart

# ── HTTP client (async OpenRouter calls) ────────────────
httpx

# ── PDF Processing ──────────────────────────────────────
pdfplumber

# ── LangChain + FAISS ───────────────────────────────────
langchain
langchain-community
langchain-core
faiss-cpu

# ── Embeddings (sentence-transformers, runs locally/free) 
sentence-transformers
torch            # CPU-only; use torch==2.3.1+cu118 for GPU
transformers
