# ── Web Framework ──────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── HTTP client (async OpenRouter calls) ────────────────
httpx==0.27.0

# ── PDF Processing ──────────────────────────────────────
pdfplumber==0.11.0

# ── LangChain + FAISS ───────────────────────────────────
langchain==0.2.5
langchain-community==0.2.5
langchain-core==0.2.9
faiss-cpu==1.8.0

# ── Embeddings (sentence-transformers, runs locally/free) 
sentence-transformers==3.0.1
torch==2.3.1            # CPU-only; use torch==2.3.1+cu118 for GPU
transformers==4.41.2
